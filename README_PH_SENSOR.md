# ESP32 pH Sensor Monitor with MQTT

Программа для мониторинга pH воды с использованием датчика PH-4502C и ESP32 с отправкой данных через MQTT.

## 📋 Требования

### Оборудование
- **ESP32 DevKit** (любая версия)
- **Датчик PH-4502C**
- Калибровочные растворы pH 4.0 и pH 7.0 (опционально)
- USB кабель для программирования
- Блок питания 5V

### Программное обеспечение
- **PlatformIO** (рекомендуется) или Arduino IDE
- Библиотеки (автоматически устанавливаются через PlatformIO):
  - PubSubClient v2.8+
  - ArduinoJson v6.21+

## 🔌 Схема подключения

```
PH-4502C Module          ESP32 DevKit
─────────────────        ────────────
VCC (5V)        ────────► 5V/VIN
GND             ────────► GND
Po (Analog Out) ────────► GPIO 34 (ADC1_CH6)
```

### Важные замечания по подключению:
- **GPIO 34** - используется как аналоговый вход (только INPUT, нет pull-up резисторов)
- Альтернативные пины: GPIO 35, 36, 39 (только INPUT)
- ⚠️ **Не используйте** GPIO 0, 2, 12, 15 (используются при загрузке)
- ⚠️ **Не используйте** ADC2 пины (GPIO 25, 26, 27...) при активном Wi-Fi

## ⚙️ Настройка программы

### 1. Настройка Wi-Fi

Откройте `src/main.cpp` и измените:

```cpp
const char* WIFI_SSID = "YOUR_WIFI_SSID";           // Ваш SSID
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";   // Ваш пароль
```

### 2. Настройка MQTT (опционально)

MQTT брокер по умолчанию: `192.168.1.173:1883`

Для изменения:

```cpp
const char* MQTT_BROKER = "192.168.1.173";  // IP вашего брокера
const int MQTT_PORT = 1883;
const char* MQTT_CLIENT_ID = "ESP32_pH_Sensor";
```

### 3. MQTT топики

- **Данные pH**: `sensor/ph/value`
  ```json
  {
    "pH": 7.15,
    "voltage": 1.523,
    "timestamp": 12345
  }
  ```

- **Статус устройства**: `sensor/ph/status`
  ```json
  {
    "status": "online",
    "device": "ESP32_pH_Sensor",
    "ip": "192.168.1.100"
  }
  ```

## 🔧 Компиляция и загрузка

### PlatformIO (рекомендуется)

```bash
# 1. Установка зависимостей
pio lib install

# 2. Компиляция
pio run

# 3. Загрузка в ESP32 (измените COM порт в platformio.ini)
pio run --target upload

# 4. Мониторинг Serial
pio device monitor
```

### Arduino IDE

1. Установите библиотеки через Library Manager:
   - PubSubClient
   - ArduinoJson

2. Откройте `src/main.cpp`

3. Выберите плату: **Tools → Board → ESP32 Dev Module**

4. Настройки платы:
   - Upload Speed: 921600
   - Flash Frequency: 80MHz
   - Flash Mode: QIO
   - Partition Scheme: Default

5. Выберите порт и загрузите программу

## 📊 Калибровка датчика

### Стандартная калибровка (по умолчанию)

Программа использует стандартные параметры для PH-4502C:

```cpp
const float ACID_VOLTAGE = 2.032;   // Напряжение при pH 4.0
const float PH_SLOPE = 0.1773;      // Наклон калибровочной кривой
```

### Точная калибровка (рекомендуется)

Для повышения точности выполните калибровку:

#### Шаг 1: Измерение в буферном растворе pH 7.0

1. Погрузите датчик в раствор pH 7.0
2. Подключитесь к Serial Monitor (115200 baud)
3. Запишите напряжение (voltage)

#### Шаг 2: Измерение в буферном растворе pH 4.0

1. Промойте датчик дистиллированной водой
2. Погрузите в раствор pH 4.0
3. Запишите напряжение

#### Шаг 3: Расчет коэффициентов

```
slope = (voltage_pH4 - voltage_pH7) / (4.0 - 7.0)
```

Пример:
- Напряжение при pH 7.0: 1.500V
- Напряжение при pH 4.0: 2.032V
- slope = (2.032 - 1.500) / (4.0 - 7.0) = 0.1773

#### Шаг 4: Обновите параметры в коде

```cpp
const float ACID_VOLTAGE = 2.032;      // Ваше напряжение при pH 4.0
const float PH_SLOPE = 0.1773;         // Ваш расчетный slope
```

## 🏠 Интеграция с Home Assistant

### Конфигурация sensor

Добавьте в `configuration.yaml`:

```yaml
mqtt:
  sensor:
    - name: "pH Sensor"
      unique_id: "esp32_ph_sensor_value"
      state_topic: "sensor/ph/value"
      value_template: "{{ value_json.pH }}"
      unit_of_measurement: "pH"
      device_class: "ph"
      state_class: "measurement"
      icon: "mdi:ph"
      json_attributes_topic: "sensor/ph/value"
      json_attributes_template: "{{ {'voltage': value_json.voltage, 'timestamp': value_json.timestamp} | tojson }}"
      
    - name: "pH Sensor Voltage"
      unique_id: "esp32_ph_sensor_voltage"
      state_topic: "sensor/ph/value"
      value_template: "{{ value_json.voltage }}"
      unit_of_measurement: "V"
      device_class: "voltage"
      state_class: "measurement"
      icon: "mdi:flash"
      
    - name: "pH Sensor Status"
      unique_id: "esp32_ph_sensor_status"
      state_topic: "sensor/ph/status"
      value_template: "{{ value_json.status }}"
      icon: "mdi:check-network"
```

### Lovelace UI карточка

```yaml
type: entities
title: pH Monitoring
entities:
  - entity: sensor.ph_sensor
    name: pH Value
  - entity: sensor.ph_sensor_voltage
    name: Sensor Voltage
  - entity: sensor.ph_sensor_status
    name: Status
```

### График истории

```yaml
type: history-graph
title: pH History
entities:
  - entity: sensor.ph_sensor
hours_to_show: 24
refresh_interval: 0
```

## 📈 Мониторинг и отладка

### Serial Monitor

Подключитесь к Serial Monitor (115200 baud) для просмотра:
- Статус подключения Wi-Fi
- Статус подключения MQTT
- Значения pH и напряжения
- Ошибки и предупреждения

Пример вывода:

```
======================================
ESP32 pH Sensor Monitor
======================================

[WiFi] Подключение к WiFi...
[WiFi] SSID: MyNetwork
[WiFi] Подключено!
[WiFi] IP адрес: 192.168.1.100
[WiFi] Уровень сигнала: -45 dBm

[MQTT] Настройка MQTT брокера: 192.168.1.173:1883
[MQTT] Попытка подключения (1/5)...
[MQTT] Подключено к брокеру!
[MQTT] Client ID: ESP32_pH_Sensor

Система готова к работе!
======================================

--- Измерение pH ---
Напряжение: 1.523 V
pH: 7.15
--------------------
[MQTT] Данные опубликованы в топик: sensor/ph/value
[MQTT] Payload: {"pH":7.15,"voltage":1.523,"timestamp":12345}
```

## 🔍 Устранение неполадок

### Проблема: Нестабильные показания

**Решения:**
- Увеличьте количество усреднений (`NUM_SAMPLES = 20`)
- Проверьте качество соединений
- Убедитесь, что датчик правильно откалиброван
- Датчик должен быть влажным

### Проблема: Не подключается к Wi-Fi

**Решения:**
- Проверьте SSID и пароль
- Убедитесь, что ESP32 в зоне покрытия
- Проверьте, что роутер поддерживает 2.4 GHz
- Перезагрузите роутер

### Проблема: Не подключается к MQTT

**Решения:**
- Проверьте IP адрес MQTT брокера
- Убедитесь, что порт 1883 открыт
- Проверьте firewall настройки
- Убедитесь, что MQTT брокер работает

### Проблема: Неправильные значения pH

**Решения:**
- Выполните калибровку датчика
- Проверьте напряжение питания (должно быть 5V)
- Замените электрод, если он изношен
- Проверьте, что электрод хранится правильно

## 📚 Технические характеристики

### PH-4502C Модуль
- Диапазон измерения: pH 0-14
- Рабочая температура: 0-60°C
- Точность: ±0.1 pH (после калибровки)
- Время отклика: < 1 минута
- Напряжение питания: 5V DC
- Выходной сигнал: 0-3.3V (аналоговый)

### ESP32 ADC
- Разрешение: 12-bit (0-4095)
- Входное напряжение: 0-3.3V
- Встроенное усреднение: 10 измерений
- Частота опроса: каждые 30 секунд

## 🛡️ Функции безопасности

- Автоматическое переподключение к Wi-Fi
- Автоматическое переподключение к MQTT
- Ограничение значений pH (0-14)
- Усреднение измерений для стабильности
- Статус мониторинг через MQTT
- Watchdog таймер (встроенный в ESP32)

## 📝 Примечания

- Храните pH электрод в специальном растворе для хранения
- Калибруйте датчик раз в месяц для точности
- Промывайте электрод дистиллированной водой после каждого использования
- Не допускайте полного высыхания электрода
- Защищайте электронику от воды

## 📄 Лицензия

Этот проект является открытым и может быть свободно использован и модифицирован.

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Serial Monitor для диагностики
2. MQTT топики в Home Assistant
3. Подключение датчика
4. Калибровку датчика

---

**Версия:** 1.0  
**Дата:** Декабрь 2025  
**Автор:** ESP32 pH Monitor System

